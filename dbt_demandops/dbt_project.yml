name: dbt_demandops
version: 1.0.0
profile: demandops_duckdb

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  dbt_demandops:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table
